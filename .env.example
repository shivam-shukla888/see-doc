# Seedoc Database Configuration
POSTGRES_USER=seedoc
POSTGRES_PASSWORD=seedoc_dev_password
POSTGRES_DB=seedoc
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# SQLAlchemy Connection URL (Port 5433 to avoid conflict with native Windows PostgreSQL service)
DATABASE_URL=postgresql+psycopg://seedoc:seedoc_dev_password@localhost:5433/seedoc
